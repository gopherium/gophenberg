version: "2"

linters:
  default: standard
  enable:
    - depguard
    - lll
    - misspell
    - revive
    - unconvert
    - unparam
  settings:
    lll:
      line-length: 120
    revive:
      rules:
        - name: blank-imports
          disabled: true
        - name: exported
    depguard:
      rules:
        sdk-purity:
          list-mode: strict
          files:
            - "**/sdk/*.go"
          allow:
            - $gostd
            - github.com/google/uuid
            - github.com/gopherium/pluginkit
        plugins-import-only-the-sdk:
          list-mode: lax
          files:
            - "**/plugins/**/*.go"
            - "!$test"
          deny:
            - pkg: github.com/gopherium/gophenberg/internal
              desc: plugins may import only the sdk package from Gophenberg

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/gopherium/gophenberg
